import React from 'react';
import PrimaryButton from './PrimaryButton';

export const RedButton = (props) => {
    return (
        <PrimaryButton borderColor='#FF2530' {...props} />
    );
};

export default RedButton;